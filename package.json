{
  "name": "create-release",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "pack": "rm -rf dist && mkdir dist && npx ncc build index.js --license licenses.txt"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@actions/core": "^1.10.0",
    "@actions/github": "^5.1.1",
    "@octokit/action": "^5.0.2"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.36.1"
  }
}
